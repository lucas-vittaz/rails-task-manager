<h1>List all tasks :</h1>

  <%@tasks.each do |task|%>
  <div>
        <input type="checkbox" id="check" name="task">
        <label for="task"><%= link_to task.title, task_path(task.id) %></label>
  <div>
  <% end %>

  <%= link_to 'Add new task', new_task_path %>
